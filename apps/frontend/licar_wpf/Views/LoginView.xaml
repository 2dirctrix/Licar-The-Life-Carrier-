<UserControl x:Class="licar_wpf.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             FontSize="16">
    <Grid Background="WhiteSmoke">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
            <TextBlock Text="로그인" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30" />

            <StackPanel Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <RadioButton Grid.Column="0" Content="간호사" GroupName="Role" HorizontalAlignment="Center" IsChecked="{Binding IsNurseRole, Mode=TwoWay}" Margin="0,0,20,0" />
                    <RadioButton Grid.Column="1" Content="약  사" GroupName="Role" HorizontalAlignment="Center"/>
                </Grid>
            </StackPanel>

            <TextBlock Text="사번" TextAlignment="Center" Margin="0,0,0,5" />
            <TextBox Text="{Binding UserId, UpdateSourceTrigger=PropertyChanged}" TextAlignment="Center" Padding="5" Margin="0,0,0,15" />

            <TextBlock Text="이름" TextAlignment="Center" Margin="0,0,0,5" />
            <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" TextAlignment="Center" Padding="5" Margin="0,0,0,25" />

            <Button Content="로그인" Command="{Binding LoginCommand}" Padding="10" IsDefault="True" Background="DodgerBlue" Foreground="White" FontWeight="Bold" />
        </StackPanel>
    </Grid>
</UserControl>
