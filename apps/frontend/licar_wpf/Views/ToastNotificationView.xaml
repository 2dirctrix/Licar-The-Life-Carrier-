<UserControl x:Class="licar_wpf.Views.ToastNotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:licar_wpf.Views"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="450">
    <Grid>
        <Border Background="#444" BorderBrush="DodgerBlue" BorderThickness="1" CornerRadius="5" Margin="10" MaxWidth="400">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="2" Opacity="0.5" BlurRadius="10" />
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding Message}" Foreground="White" FontSize="20"
                           VerticalAlignment="Center" Margin="15,10" TextWrapping="Wrap" />
                <Button Grid.Column="1" Content="X" Command="{Binding CloseCommand}" FontWeight="Bold"
                        Foreground="White" Background="Transparent" BorderThickness="0" VerticalAlignment="Top"
                        Margin="0,5,10,0" Cursor="Hand" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
